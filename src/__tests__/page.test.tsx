// We NEED tests... for now we need to keep moving

import '@testing-library/jest-dom';

describe('Page', () => {
  it('renders a heading', () => {
    // render(<Page />)

    // const heading = screen.getByRole('heading', { level: 1 })

    // expect(heading).toBeInTheDocument()

    expect(true).toBeTruthy();
  });
});
