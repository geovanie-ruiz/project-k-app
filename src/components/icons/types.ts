export type IconProps = {
  isNormal?: boolean;
  isMedium?: boolean;
  isLarge?: boolean;
  isXLarge?: boolean;
};

export type RuneIconProps = {
  isNormal?: boolean;
  isMedium?: boolean;
  isLarge?: boolean;
  isXLarge?: boolean;
  isComplex?: boolean;
};
